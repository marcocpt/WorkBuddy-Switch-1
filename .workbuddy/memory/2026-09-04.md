# 2026-09-04 工作日志

## 当前会话 chatgpt conversation_id
- `wb-20260904-3d91`（本会话所有 chatgpt_send 复用此 id）

## 承接 9/3 的 Bug 修复任务（WorkBuddy 恢复会话报「未找到 WorkBuddy」）
- 9/3 已完成：修复 1230a51 → 文档 3951e18 → 合并 develop 4a4798a(--no-ff)；ChatGPT 三轮复核全 CLOSED；仅剩远程 push 被 GitHub 403 拦截（凭据 marcocpt 对 koi128bit/WorkBuddy-Switch 无写权限）

## 本轮关键发现（影响修复落点）
- 用户**实际运行**的是 `/Applications/workbuddy-switch.app`（bundle `com.wbswitch.app`，二进制 `wb-switch-rust`，v0.1.30，9/2 构建），**不是**本 Swift 仓库产出的 `/Applications/WorkBuddy Switch.app`（`com.koi128bit.openusage`，v0.2.0）
- `strings wb-switch-rust` 检出硬编码 `com.workbuddy.workbuddy`（旧 ID，用于 osascript quit），**未检出** `com.tencent.workbuddy.mac` → **Rust 版存在同一根因**
- Rust 版源码不在 ~/Working 下（grep `com.wbswitch.app` 无结果、无 Cargo.toml）；`workbuddy-account-hub`（Python+Tauri）与 `zcode-switcher`（ZCode 产品）均非其源码

## 环境经验（可复用）
- 本仓库 `scripts/build-release.sh` 依赖 SPM，`swift build` 因 `sandbox-exec: sandbox_apply: Operation not permitted` 在此环境不可用；改用 `swiftc` 直编全部 Sources/OpenUsage/*.swift（-framework AppKit/SwiftUI/Security -lsqlite3）可成功产出二进制
- iconutil 生成 icns 在本环境失败（iconset 校验不过），验证构建可跳过 icns
- 验证构建产物：`dist/WorkBuddy Switch.app` 0.2.1-fix (build 7)，ad-hoc 签名通过
